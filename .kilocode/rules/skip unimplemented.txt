# skip unimplemented.txt

If the python bindings is not implemented, finish the moving task.

## Guidelines

- Read the task file corresponding to the Java file name.
- If the task file shows that the file is not suitable for python bindings finish the copying task.
- If the Java contains only interpretations without instansiations, and you can't find the bindings, finish the copying task.
- If there are no python bindings implemented in uacal_lib/src/alg.rs.backup you don't have to copy, you can finish the copying task.
